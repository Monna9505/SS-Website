import Link from 'next/link';
import client from '../connection';
import { useEffect, useState } from 'react';

export default function Footer({headerLinks}:any) {

    return (
        <footer>
            
        </footer>
    );
}